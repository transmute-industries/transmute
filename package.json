{
  "license": "MIT",
  "devDependencies": {
    "lerna": "^2.5.1"
  },
  "scripts": {
    "coverage": "lerna run test:coverage:report --scope transmute-framework",
    "travis": "lerna run replace_and_test --scope transmute-framework"
  }
}
