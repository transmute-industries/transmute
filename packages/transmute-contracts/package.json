{
  "name": "transmute-contracts",
  "version": "0.1.3",
  "description": "",
  "main": "truffle.js",
  "scripts": {
    "cleanup": "rm -rf ./build",
    "build": "npm run test:coverage && npm run migrate",
    "test": "./node_modules/.bin/solidity-coverage",
    "test:coverage:report": "codecov",
    "migrate": "rm -rf  ./build && truffle migrate --reset"
  },
  "license": "MIT",
  "devDependencies": {
    "accesscontrol": "^2.2.0",
    "bs58": "^4.0.1",
    "codecov": "^3.0.0",
    "ethereumjs-util": "^5.1.2",
    "solidity-coverage": "^0.3.5",
    "web3-utils": "^1.0.0-beta.26"
  }
}
