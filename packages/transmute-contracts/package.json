{
  "name": "transmute-contracts",
  "version": "0.1.4-alpha.0.3",
  "description": "",
  "main": "truffle.js",
  "scripts": {
    "cleanup": "rm -rf ./build",
    "build": "npm run contracts:migrate",
    "contracts:migrate": "npm run cleanup && truffle migrate --network ganache --reset",
    "contracts:migrate:ganache": "NODE_TLS_REJECT_UNAUTHORIZED=0 truffle migrate --network ganache --reset"
  },
  "license": "MIT",
  "devDependencies": {
    "bs58": "^4.0.1",
    "codecov": "^3.0.0",
    "ethereumjs-util": "^5.1.2",
    "web3-utils": "^1.0.0-beta.26"
  },
  "dependencies": {
    "@digix/truffle-lightwallet-provider": "^0.1.4"
  }
}
