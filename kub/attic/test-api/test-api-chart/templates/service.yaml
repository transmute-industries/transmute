apiVersion: v1
kind: Service
metadata:
  name: {{ template "test-api-chart.fullname" . }}
  labels:
    app: {{ template "test-api-chart.name" . }}
    chart: {{ template "test-api-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "test-api-chart.name" . }}
    release: {{ .Release.Name }}
