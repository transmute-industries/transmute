matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
      env:
        - DO_JWT_DL=n
        - CHANGE_MINIKUBE_NONE_USER=true
        - MINIKUBE_VERSION=v0.25.2
        - USE_VOX=n
        - USE_WARN=n
        - EDITOR=cat
        - MINIKUBE_MEMORY=7777
        - MINIKUBE_CPU=4
        - MINIKUBE_DRIVER=none
        - HELM_INSTALL_DIR=$HOME/.local/bin
        - HELM_VERSION=v2.8.2
        - NVM_DIR="$HOME/.nvm"
        - KUBE_VERSION=v1.9.0
        - HELM_SHA256=0521956fa22be33189cc825bb27b3f4178c5ce9a448368b5c81508d446472715
      language: bash
      cache:
        directories:
        - $HOME/.local/bin
        - $HOME/.minikube
        - $HOME/.kube
        - /usr/local
        - .git
#    - os: osx
#      osx_image: xcode8.3
#      language: bash
#      cache:
#        directories:
#        - $HOME/.minikube
#        - $HOME/.transmute
#        - $HOME/.kube
#      env:
#        - CHANGE_MINIKUBE_NONE_USER=true
#        - MINIKUBE_VERSION=v0.25.2
#        - USE_VOX=n

before_script:
# Download kubectl, which is a requirement for using minikube.
# Download minikube.
- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sh .ci/linux_before_install.sh; fi
  #- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sh .ci/mac_before_script.sh; fi
- bash -l -c "echo PATH=$HOME/.transmute/bin:$PATH >> $HOME/.bashrc"
- bash -l -c "echo PATH=$HOME/.local/bin:$PATH >> $HOME/.bashrc"
- if [[ ! -e "/usr/local/bin/kubectl" ]]; then curl -Lo kubectl https://storage.googleapis.com/kubernetes-release/release/$KUBE_VERSION/bin/linux/amd64/kubectl && chmod +x kubectl && sudo mv kubectl /usr/local/bin/; fi
- if [[ ! -e "/usr/local/bin/minikube" ]]; then curl -Lo minikube https://storage.googleapis.com/minikube/releases/$MINIKUBE_VERSION/minikube-linux-amd64 && chmod +x minikube && sudo mv minikube /usr/local/bin/; fi
- mkdir -p $HOME/.local/bin
- if [[ ! -e "$HOME/.local/bin/helm" ]]; then sh .ci/gethelm; fi
- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then echo "$HELM_SHA256 $HOME/.local/bin/helm" > /tmp/testhelm && sha256sum -c /tmp/testhelm && rm /tmp/testhelm; fi
- if [[ "$TRAVIS_OS_NAME" == "linux" && ! -e "$HOME/.local/bin/nsenter" ]]; then .ci/ubuntu-compile-nsenter.sh; fi
- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo cp $HOME/.local/bin/nsenter /usr/bin/nsenter; fi
- cd $HOME; rm -Rf .transmute; mkdir -p .transmute/git; cd .transmute/git; ln -s $TRAVIS_BUILD_DIR transmute 
- cd $TRAVIS_BUILD_DIR
- TRANSMUTE_RELEASE=$TRAVIS_BRANCH bash -l ./bootstrap
- export PATH=$HOME/.transmute/bin:$PATH
- cd $TRAVIS_BUILD_DIR/packages/transmute-cli
- bash -l -c "npm i"
- bash -l -c "npm run build"
- bash -l -c "npm i -g"
- cd $TRAVIS_BUILD_DIR

script:
- bash -l -c "npm i"
- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then bash -l -c "transmute k8s provision --minikube --vmdriver none"; fi
- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then bash -l -c "yes y|transmute k8s init --minikube"; fi
- lerna bootstrap
- lerna run --scope transmute-framework truffle:test
- lerna run --scope transmute-framework truffle:migrate
- lerna run --scope transmute-framework test
- lerna run --scope transmute-framework test:report
